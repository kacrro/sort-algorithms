# Window parameters
WINDOW_TITLE = "fajne oknienko do sortowania liczb"
WINDOW_WIDTH = 1600
WINDOW_HEIGHT = 800
WINDOW_SIZE = f"{WINDOW_WIDTH}x{WINDOW_HEIGHT}"
WINDOW_BG_COLOR = "#000000"

# Canvas parameters
CANVAS_WIDTH = ((WINDOW_WIDTH - 20) / 2) * 0.9
CANVAS_HEIGHT = ((WINDOW_HEIGHT - 100) / 2) * 0.9

# Data parameters
BAR_COUNT = 60
a = 1   # Minimal value for the data
b = 100 # Maximal value for the data
RANGE = (a, b)

delay = 1  # Delay in milliseconds for the sorting animation
