import tkinter as tk

max_height = 100

root = tk.Tk() # Create the main window
root.title("Sorting Algorithms") # Set the window title
root.geometry("800x600") # Set the window size

Canvas = tk.Canvas(root, width=800, height=600, bg="black") # Create a canvas
Canvas.pack() # Pack the canvas into the window




root.mainloop()